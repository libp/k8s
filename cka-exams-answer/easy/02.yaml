# 将一个名为ek8s-node-1的节点设置为不可用并将其上的pod重新调度

# 分数：4

# 关键字：drain
# 链接：https://kubernetes.io/zh-cn/docs/tasks/administer-cluster/safely-drain-node/

# 将节点置为不可用
# kubectl drain k8s-worker1 --ignore-daemonsets

# 恢复节点
# kubectl uncordon k8s-worker1